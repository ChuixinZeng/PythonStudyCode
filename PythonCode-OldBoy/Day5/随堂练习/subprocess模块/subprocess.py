# -*- coding:utf-8 -*-

# Author:Chuixin Zeng

# subprocess是对os.system os.spawn*的替换，以后这两个命令就没有了

# 用法：subprocess.run(args, *, stdin=None, input=None, stdout=None, stderr=None, shell=False, timeout=None, check=False)

# 在windows平台上面演示不了，只能在Linux上面使用
